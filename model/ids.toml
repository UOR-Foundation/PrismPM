spec = "prismpm/ids/1"

[[id]]
id = "RP-01"
suite = "repository"
level = "build"
statement = "PrismPM is structured as a virtual Rust workspace with pinned toolchains."

[[id]]
id = "RP-02"
suite = "repository"
level = "build"
statement = "Toolchain versions for Rust and Lean are pinned in rust-toolchain.toml and lean-toolchain."

[[id]]
id = "RP-03"
suite = "repository"
level = "build"
statement = "Every model claim is authored in model/*.toml and validated by cargo xtask validate-model."

[[id]]
id = "RP-04"
suite = "repository"
level = "build"
statement = "The devcontainer and CI enforce hermetic build and test environments."

[[id]]
id = "RP-05"
suite = "repository"
level = "build"
statement = "All public crates forbid unsafe code and enforce strict lints."

[[id]]
id = "RP-06"
suite = "repository"
level = "build"
statement = "The Justfile exposes vv as the primary acceptance gate."

[[id]]
id = "RP-07"
suite = "repository"
level = "build"
statement = "The SPEC.md capability table and model/ids.toml are bijective and consistent."

[[id]]
id = "RP-08"
suite = "repository"
level = "build"
statement = "Every public capability has exactly one Gherkin scenario and one named conformance test."

[[id]]
id = "RP-09"
suite = "repository"
level = "build"
statement = "Two clean builds in distinct absolute directories produce byte-identical platform-independent outputs."

[[id]]
id = "RP-10"
suite = "repository"
level = "build"
statement = "All generated documentation is regenerated and validated against model registers."

[[id]]
id = "RP-11"
suite = "repository"
level = "build"
statement = "License metadata and files conform to dual MIT/Apache-2.0 requirements."

[[id]]
id = "RP-12"
suite = "repository"
level = "build"
statement = "Release artifacts are refused unless all release criteria and verifications hold."

[[id]]
id = "FT-01"
suite = "facets"
level = "build"
statement = "prism.arch defines ISO/IEC/IEEE 42010:2022 architecture description concepts in LexLean."

[[id]]
id = "FT-02"
suite = "facets"
level = "build"
statement = "prism.sec defines ISO/IEC 27034 application security concepts in LexLean."

[[id]]
id = "FT-03"
suite = "facets"
level = "build"
statement = "prism.sec defines ISO/IEC 27034-5 application security control data structures."

[[id]]
id = "FT-04"
suite = "facets"
level = "build"
statement = "prism.sec defines ISO/IEC 27005:2022 information security risk management concepts."

[[id]]
id = "FT-05"
suite = "facets"
level = "build"
statement = "prism.qual defines ISO/IEC 25010:2023 product quality characteristics and requirements."

[[id]]
id = "FT-06"
suite = "facets"
level = "build"
statement = "Facet packages use closed lexical signatures and renderer-token coverage."

[[id]]
id = "FT-07"
suite = "facets"
level = "build"
statement = "Shared terms across facets have unique ownership and acyclic dependencies."

[[id]]
id = "FT-08"
suite = "facets"
level = "build"
statement = "Every standards entry links to exactly one row in model/standards.toml."

[[id]]
id = "FT-09"
suite = "facets"
level = "build"
statement = "Facet lexicons produce deterministic canonical LaTeX and Lean lowerings."

[[id]]
id = "FT-10"
suite = "facets"
level = "build"
statement = "Lexicon package locks reproduce across multiple directory roots."

[[id]]
id = "HO-01"
suite = "holo"
level = "build"
statement = "A Holo/1 artifact is a strict binary Hologram archive with physical version 4 and the HOLO header."

[[id]]
id = "HO-02"
suite = "holo"
level = "build"
statement = "Legacy application and architecture model documents retain the closed prismpm/model-document/1 schema and model.prism.json name."

[[id]]
id = "HO-03"
suite = "holo"
level = "build"
statement = "Prism model documents use deterministic canonical JSON and an exact SHA-256 model identity."

[[id]]
id = "HO-04"
suite = "holo"
level = "build"
statement = "The model-document projector is a total deterministic function from valid LexLean semantic snapshots."

[[id]]
id = "HO-05"
suite = "holo"
level = "build"
statement = "Model entity identifiers are qualified strings assigned deterministic zero-based indexes."

[[id]]
id = "HO-06"
suite = "holo"
level = "build"
statement = "Platform-independent Prism model and Holo artifacts exclude host paths, timestamps, and unstable environment data."

[[id]]
id = "HO-07"
suite = "holo"
level = "build"
statement = "Holo decoding rejects legacy JSON, noncanonical archives, malformed values, and unsupported physical versions."

[[id]]
id = "HO-08"
suite = "holo"
level = "build"
statement = "The emitter-semantics ID uniquely identifies the model-document projector inputs."

[[id]]
id = "HO-09"
suite = "holo"
level = "build"
statement = "Shared goldens bind generated Lean, canonical model documents, and binary Holo application projections to their sources."

[[id]]
id = "HO-10"
suite = "holo"
level = "build"
statement = "Holo/1 validation checks canonical sections, identities, content closure, directory derivation, and closed Prism provenance."

[[id]]
id = "HO-11"
suite = "holo"
level = "build"
statement = "The explicit text application profile projects to closed model-document/2, retaining legacy model-document/1 and rejecting invalid UTF-8 response, field, root, and byte-bound declarations."

[[id]]
id = "HO-12"
suite = "holo"
level = "build"
statement = "Application verification executes the exact portable View in Chromium through the authoritative Hologram intent and Core-Wasm session, rejecting incomplete browser evidence."

[[id]]
id = "CT-01"
suite = "controller"
level = "build"
statement = "The Controller API exposes owned request and result types for load, check, and build."

[[id]]
id = "CT-02"
suite = "controller"
level = "build"
statement = "The Controller encapsulates LexLean Engine operations without exposing internal compiler types."

[[id]]
id = "CT-03"
suite = "controller"
level = "build"
statement = "prismpm check validates models in memory without modifying the filesystem."

[[id]]
id = "CT-04"
suite = "controller"
level = "build"
statement = "prismpm build atomically publishes artifacts under content-addressed build directories."

[[id]]
id = "CT-05"
suite = "controller"
level = "build"
statement = "The CLI produces structured machine-readable JSON output on stdout."

[[id]]
id = "CT-06"
suite = "controller"
level = "build"
statement = "The Controller enforces positive resource and entity limits."

[[id]]
id = "CT-07"
suite = "controller"
level = "build"
statement = "All public errors carry registered PP diagnostic codes."

[[id]]
id = "CT-08"
suite = "controller"
level = "build"
statement = "The Controller operates completely offline during build and check."

[[id]]
id = "CT-09"
suite = "controller"
level = "build"
statement = "Project configuration is strictly validated against schemas/project.schema.json."

[[id]]
id = "CT-10"
suite = "controller"
level = "build"
statement = "The Controller preserves cause chains for underlying LexLean diagnostics."

[[id]]
id = "CT-11"
suite = "controller"
level = "build"
statement = "Controller cleanup removes only the configured real output directory and rejects a symlink target."

[[id]]
id = "ST-01"
suite = "stdlib"
level = "build"
statement = "Prism-stdlib defines core ISO 42010 architectural primitives in Foundation.Arch."

[[id]]
id = "ST-02"
suite = "stdlib"
level = "build"
statement = "Prism-stdlib defines ISO 27034 security primitives in Foundation.Sec."

[[id]]
id = "ST-03"
suite = "stdlib"
level = "build"
statement = "Prism-stdlib defines ISO 27005 risk primitives in Foundation.Sec."

[[id]]
id = "ST-04"
suite = "stdlib"
level = "build"
statement = "Prism-stdlib defines ISO 25010 quality primitives in Foundation.Qual."

[[id]]
id = "ST-05"
suite = "stdlib"
level = "build"
statement = "Prism-stdlib is authored strictly in .lex.tex with no handwritten Lean source."

[[id]]
id = "ST-06"
suite = "stdlib"
level = "build"
statement = "Prism-stdlib models allow cyclic component graphs while rejecting dangling references."

[[id]]
id = "ST-07"
suite = "stdlib"
level = "build"
statement = "Prism-stdlib proves cross-facet consistency theorems with empty observed axiom sets."

[[id]]
id = "ST-08"
suite = "stdlib"
level = "build"
statement = "Prism-stdlib exports registered runtime validator roots."

[[id]]
id = "ST-09"
suite = "stdlib"
level = "build"
statement = "Prism-stdlib includes golden test outputs for all published artifacts."

[[id]]
id = "ST-10"
suite = "stdlib"
level = "build"
statement = "Prism-stdlib models validate through the Holo projector and Lean kernel."

[[id]]
id = "ST-11"
suite = "stdlib"
level = "build"
statement = "Saved-code recovery transitions bind admitted current credentials, consume codes atomically, preserve authorization, and reject stale or substituted recovery evidence."

[[id]]
id = "ST-12"
suite = "stdlib"
level = "build"
statement = "The internal scoped-administration reducer preserves revision-bound approval and complete post-change ownership in generated native execution."

[[id]]
id = "ST-13"
suite = "stdlib"
level = "build"
statement = "The internal mailbox-admission kernel binds profile-specific admitted proof to current authority, account, challenge and credential state in generated native execution."

[[id]]
id = "ST-14"
suite = "stdlib"
level = "build"
statement = "The internal candidate browser-bootstrap kernel binds admitted peer sessions to explicit public-operator policy, consent, bounded reservations and fail-closed channel lifecycle in generated native execution."

[[id]]
id = "ST-15"
suite = "stdlib"
level = "build"
statement = "The internal organization lifecycle creates isolated provisional organizations without name privileges and composes scoped administration for revision-bound activation and founding-grant handover."

[[id]]
id = "ST-16"
suite = "stdlib"
level = "build"
statement = "The internal bounded CBOR primitive profile preserves deterministic encoding, typed cursor limits and strict UTF-8 through generated native and bounded Wasm execution."

[[id]]
id = "AR-01"
suite = "artifacts"
level = "build"
statement = "Build artifacts are published under content-addressed .prism/build/<id> paths."

[[id]]
id = "AR-02"
suite = "artifacts"
level = "build"
statement = "Every build directory contains a canonical manifest of file paths, sizes, and hashes."

[[id]]
id = "AR-03"
suite = "artifacts"
level = "build"
statement = "Artifact content IDs are derived from deterministic SHA-256 digests."

[[id]]
id = "AR-04"
suite = "artifacts"
level = "build"
statement = "Published artifacts contain no absolute paths, timestamps, or host identifiers."

[[id]]
id = "AR-05"
suite = "artifacts"
level = "build"
statement = "Artifact publication is atomic and leaves no partial state on failure."

[[id]]
id = "AR-06"
suite = "artifacts"
level = "build"
statement = "Rebuilding an identical project verifies existing artifacts before reuse."

[[id]]
id = "AR-07"
suite = "artifacts"
level = "build"
statement = "Tampered build artifacts are detected and rejected."

[[id]]
id = "AR-08"
suite = "artifacts"
level = "build"
statement = "Canonical LaTeX and Lean artifacts reproduce byte-for-byte across runs."

[[id]]
id = "AR-09"
suite = "artifacts"
level = "build"
statement = "Source maps provide complete token traceability between .lex.tex and generated Lean."

[[id]]
id = "AR-10"
suite = "artifacts"
level = "build"
statement = "Coverage reports document all modeled and unmodeled elements."

[[id]]
id = "EX-01"
suite = "execution"
level = "build"
statement = "lean4-prod exports compilable LCNF from LexLean-generated Lean modules."

[[id]]
id = "EX-02"
suite = "execution"
level = "build"
statement = "lean4-prod transitive closure extraction resolves all dependencies without opaque gaps."

[[id]]
id = "EX-03"
suite = "execution"
level = "build"
statement = "Generated Rust validators are no_std compatible and perform no heap allocation during validator calls."

[[id]]
id = "EX-04"
suite = "execution"
level = "build"
statement = "The Rust host strictly decodes canonical Holo and normalizes string IDs to indexes."

[[id]]
id = "EX-05"
suite = "execution"
level = "build"
statement = "Generated Rust validators correctly evaluate normalized Holo models."

[[id]]
id = "EX-06"
suite = "execution"
level = "build"
statement = "Exhaustive generated-Rust evaluation matches the expected truth tables stated and proved in .lex.tex for the fixed small-model bounds."

[[id]]
id = "EX-07"
suite = "execution"
level = "build"
statement = "Deterministic property tests with committed seeds exercise validator behavior within the recorded finite strategies and bounds."

[[id]]
id = "EX-08"
suite = "execution"
level = "build"
statement = "Generated Rust validators never panic on caller-controlled inputs."

[[id]]
id = "EX-09"
suite = "execution"
level = "build"
statement = "Erased proof fields are not claimed as runtime checks."

[[id]]
id = "EX-10"
suite = "execution"
level = "build"
statement = "Type boundary conversions between Lean Nat and Rust u64 are explicitly bounded."

[[id]]
id = "VR-01"
suite = "verification"
level = "build"
statement = "LexLean verifies formal models against the Lean 4.32.1 kernel."

[[id]]
id = "VR-02"
suite = "verification"
level = "build"
statement = "Verification records executable digests and normalized toolchain commands."

[[id]]
id = "VR-03"
suite = "verification"
level = "build"
statement = "Lake stages generated Lean modules in isolated temporary workspaces."

[[id]]
id = "VR-04"
suite = "verification"
level = "build"
statement = "leanchecker replays compiled module environments independently."

[[id]]
id = "VR-05"
suite = "verification"
level = "build"
statement = "Axiom audits enforce that all PrismPM-owned theorems have empty observed axiom sets."

[[id]]
id = "VR-06"
suite = "verification"
level = "build"
statement = "Controller::verify publishes verified attestations under .prism/verified/<id>."

[[id]]
id = "VR-07"
suite = "verification"
level = "build"
statement = "Verification manifests bind the Prism model document, Lean, LCNF, and Rust execution evidence."

[[id]]
id = "VR-08"
suite = "verification"
level = "build"
statement = "Verification fails if any Lean elaboration, replay, or axiom check fails."

[[id]]
id = "VR-09"
suite = "verification"
level = "build"
statement = "Verification performs no hidden network access."

[[id]]
id = "VR-10"
suite = "verification"
level = "build"
statement = "Process execution limits output size and execution time strictly."

[[id]]
id = "VR-11"
suite = "verification"
level = "build"
statement = "Planted defects in proofs or validators reliably trigger verification failure."

[[id]]
id = "VR-12"
suite = "verification"
level = "build"
statement = "Repeated fixed-seed verification runs produce identical attestation digests."

[[id]]
id = "SE-01"
suite = "security"
level = "build"
statement = "All filesystem operations are confined within configured output roots."

[[id]]
id = "SE-02"
suite = "security"
level = "build"
statement = "Symlinks and relative paths that escape project boundaries are rejected."

[[id]]
id = "SE-03"
suite = "security"
level = "build"
statement = "Process execution executes child binaries directly without invoking a shell."

[[id]]
id = "SE-04"
suite = "security"
level = "build"
statement = "Environment variables passed to child processes are sanitized and normalized."

[[id]]
id = "SE-05"
suite = "security"
level = "build"
statement = "File publications use atomic rename operations to prevent partial writes."

[[id]]
id = "SE-06"
suite = "security"
level = "build"
statement = "Memory safety is enforced across all Rust crates with unsafe code forbidden."

[[id]]
id = "SE-07"
suite = "security"
level = "build"
statement = "Input parsing rejects excessively large inputs before memory exhaustion."

[[id]]
id = "SE-08"
suite = "security"
level = "build"
statement = "Dependency audits with cargo-deny enforce license and advisory policies."

[[id]]
id = "AU-01"
suite = "authorities"
level = "build"
statement = "Every standards claim uses a typed immutable authority binding distinct from its executable oracle."

[[id]]
id = "AU-02"
suite = "authorities"
level = "build"
statement = "Authority resolution writes a canonical reviewable lock and locked resolution rejects drift."

[[id]]
id = "AU-03"
suite = "authorities"
level = "build"
statement = "Fetch verifies signatures and digests before atomically publishing immutable cached inputs."

[[id]]
id = "AU-04"
suite = "authorities"
level = "build"
statement = "Authority and oracle verification reproduces from a populated cache with network access disabled."

[[id]]
id = "AU-05"
suite = "authorities"
level = "build"
statement = "Oracle runners are digest-bound, resource-bounded, networkless, and report exact covered and uncovered requirements."

[[id]]
id = "AU-06"
suite = "authorities"
level = "build"
statement = "Official positive and negative corpora plus Prism mutations detect stale, bypassed, changed, or always-passing oracles."

[[id]]
id = "SY-01"
suite = "system"
level = "build"
statement = "Prism system models cover product, components, interfaces, topology, configuration, data, operations, and lifecycle."

[[id]]
id = "SY-02"
suite = "system"
level = "build"
statement = "System validation enforces closure, uniqueness, references, compatibility, capabilities, ordering, and release completeness."

[[id]]
id = "SY-03"
suite = "system"
level = "build"
statement = "All Prism system semantics and validators are authored in LexLean and exported only from generated Lean roots."

[[id]]
id = "SY-04"
suite = "system"
level = "build"
statement = "System models deterministically project OpenAPI, AsyncAPI, CloudEvents, SPDX, OpenTelemetry, Compose, and Kubernetes inputs."

[[id]]
id = "SY-05"
suite = "system"
level = "build"
statement = "Late-bound configuration is typed and secret values remain external references excluded from artifacts and evidence."

[[id]]
id = "SY-06"
suite = "system"
level = "build"
statement = "Migrations, recovery, rollout, rollback, drift, retirement, and positive and negative acceptance are explicit modeled values."

[[id]]
id = "SY-07"
suite = "system"
level = "build"
statement = "Modeled ordered control coverage rejects missing obligations, invalid inheritance, residual omissions, and binding mismatches."

[[id]]
id = "DK-01"
suite = "sdk"
level = "build"
statement = "One versioned SDK inventory closes over PrismPM, LexLean, Lean, lean4-prod, stdlib, oracles, adapters, and schemas."

[[id]]
id = "DK-02"
suite = "sdk"
level = "build"
statement = "Signed non-root amd64 and arm64 SDK images use digest-pinned bases and carry OCI, SPDX, and provenance evidence."

[[id]]
id = "DK-03"
suite = "sdk"
level = "build"
statement = "Native Linux archives and OCI SDK execution produce identical platform-independent outputs and diagnostics."

[[id]]
id = "DK-04"
suite = "sdk"
level = "build"
statement = "The complete SDK lock and explicit fetch phase permit all build and verification phases to run locked and offline."

[[id]]
id = "DK-05"
suite = "sdk"
level = "build"
statement = "SDK bootstrap uses the prior accepted SDK, two clean self-rebuilds, and independent formal evidence verification without a trust cycle."

[[id]]
id = "DK-06"
suite = "sdk"
level = "build"
statement = "SDK execution rejects undeclared PATH tools, tampered executables, base drift, mutable inputs, and circular self-attestation."

[[id]]
id = "DK-07"
suite = "sdk"
level = "build"
statement = "The browser cryptography host boundary supplies bounded WebCrypto randomness, signs domain-separated bytes with nonextractable keys, and rejects changed bindings or unavailable cryptography without assigning organizational authority."

[[id]]
id = "DK-08"
suite = "sdk"
level = "build"
statement = "The browser storage host boundary retains identity keys and content-addressed bytes across reopening and atomically rejects stale heads, partial writes, corruption, and resource-policy changes."

[[id]]
id = "DK-09"
suite = "sdk"
level = "build"
statement = "The browser peer host boundary exchanges bounded ordered bytes over manually paired direct WebRTC sessions and rejects malformed signaling, framing, queue overflow, expired operations, and closed sessions without claiming peer authority or internet-wide discovery."

[[id]]
id = "DK-10"
suite = "sdk"
level = "build"
statement = "The LexLean workspace reducer executes its complete bounded state-transition corpus through freshly generated Rust and Core-Wasm; authentication, durable effects, and application acceptance remain separate obligations."

[[id]]
id = "DK-11"
suite = "sdk"
level = "build"
statement = "The modeled signed-envelope codec rejects malformed framing and binds generated signing projections to actual browser cryptography without treating parsing as authentication."

[[id]]
id = "DK-12"
suite = "sdk"
level = "build"
statement = "The modeled journal and private browser adapter authenticate complete replay, preserve exact bounded native/Wasm transitions, and promote state only after an atomic bound commit; conflicts and uncertain outcomes require replay without automatic branch selection."

[[id]]
id = "DK-13"
suite = "sdk"
level = "build"
statement = "Generated workspace commands bind actual identity, current head, hashes and signatures before authenticated atomic journal admission; product callers receive status or commit acknowledgments, never raw state."

[[id]]
id = "DK-14"
suite = "sdk"
level = "build"
statement = "Generated admitted queries use privately authenticated replay and possessed identity to paginate every bounded member and message row, rejecting stale or revoked contexts before disclosure."

[[id]]
id = "DK-15"
suite = "sdk"
level = "build"
statement = "The modeled workspace View preserves complete admitted rows, closed single-flight interaction, exact private correlation and replay-required recovery through fresh native, no_std and Core-Wasm execution of every bounded vector."

[[id]]
id = "DK-16"
suite = "sdk"
level = "build"
statement = "The private workspace View host dispatches only modeled effects and renders verified plain-text labels and admitted presentation; genuine browser journeys, native transcript replay and planted defects verify closure, durability, recovery and terminal failure."

[[id]]
id = "DK-17"
suite = "sdk"
level = "build"
statement = "Explicit native libraries bind typed model exports and execute every modeled acceptance root in generated std and no_std packages without claiming application or deployment acceptance."

[[id]]
id = "DK-18"
suite = "sdk"
level = "build"
statement = "The internal modeled effect protocol binds admitted guest and browser primitive requests to immutable resources and exact single-consumption completions, retaining uncertain and closed pending operations without claiming an executable application profile."

[[id]]
id = "DK-19"
suite = "sdk"
level = "build"
statement = "The browser RS256 host primitive verifies exact bounded bytes with an imported RSA public key and rejects changed signatures, weak keys and unavailable cryptography without interpreting tokens or assigning authority."

[[id]]
id = "DK-20"
suite = "sdk"
level = "build"
statement = "The internal bounded effect wire codec executes actual modeled transitions in generated native and Core-Wasm code, while a private browser host binds real guest, cryptography and storage effects without accepting caller completions or claiming application acceptance."

[[id]]
id = "OC-01"
suite = "oci"
level = "build"
statement = "Product releases use OCI 1.1 descriptors, manifests, indexes, subjects, annotations, and referrers with registered media types."

[[id]]
id = "OC-02"
suite = "oci"
level = "build"
statement = "A locked build atomically emits a verified root only after every declared source, proof, package, oracle, and release gate passes."

[[id]]
id = "OC-03"
suite = "oci"
level = "build"
statement = "The release graph closes over all artifacts and binds SBOM, provenance, validation, signature, policy, and deployment referrers to exact subjects."

[[id]]
id = "OC-04"
suite = "oci"
level = "build"
statement = "Push, pull, and inspect preserve and validate complete OCI graph closure without executing artifact content or rebuilding."

[[id]]
id = "OC-05"
suite = "oci"
level = "build"
statement = "Local and GHCR registry profiles pass claimed OCI distribution operations and fail safely under mutation, interruption, concurrency, and tag races."

[[id]]
id = "OC-06"
suite = "oci"
level = "build"
statement = "Promotion adds signed evidence around one immutable subject digest and never changes or rebuilds release content."

[[id]]
id = "OC-07"
suite = "oci"
level = "build"
statement = "Browser export replays the immutable release closure without source or execution and atomically copies only its exact browser artifacts without granting publication authority."

[[id]]
id = "OC-08"
suite = "oci"
level = "build"
statement = "Browser publication integrity replays the source-free release and compares its complete browser closure at one explicit canonical HTTPS base, rejecting redirects and bounded transport failures without claiming deployment authorization or product acceptance."

[[id]]
id = "LC-01"
suite = "lifecycle"
level = "build"
statement = "The Controller owns fetch, build, push, pull, inspect, run, plan, deploy, status, rollback, and explicit destroy operations."

[[id]]
id = "LC-02"
suite = "lifecycle"
level = "build"
statement = "Build, push, run, and deploy accept Docker-simple command forms and return stable pipe-safe canonical result values."

[[id]]
id = "LC-03"
suite = "lifecycle"
level = "build"
statement = "Local run uses unmodified OCI, container, Compose, and Hologram runtimes with modeled isolation, readiness, acceptance, signals, and shutdown."

[[id]]
id = "LC-04"
suite = "lifecycle"
level = "build"
statement = "Controller operations are bounded, cancellable, atomic, concurrent-safe, path-confined, and idempotent where declared."

[[id]]
id = "LC-05"
suite = "lifecycle"
level = "build"
statement = "All caller-controlled failure boundaries return registered diagnostics in stable public exit classes without panic."

[[id]]
id = "LC-06"
suite = "lifecycle"
level = "build"
statement = "Generated help and shell completions describe only executable command-contract examples verified in the SDK devcontainer."

[[id]]
id = "DP-01"
suite = "deployment"
level = "build"
statement = "OCI-packaged target adapters consume canonical model, release, and plan documents without introducing application behavior."

[[id]]
id = "DP-02"
suite = "deployment"
level = "build"
statement = "The Compose adapter emits and applies only pinned Compose Specification documents with declared values and capabilities."

[[id]]
id = "DP-03"
suite = "deployment"
level = "build"
statement = "The Kubernetes adapter emits Kubernetes 1.36.4 resources validated by schema, decoding, dry-run admission, Kind, and supported skew."

[[id]]
id = "DP-04"
suite = "deployment"
level = "build"
statement = "Deployment planning is deterministic, binds observed state, and reports every create, update, replacement, deletion, migration, risk, and rollback consequence."

[[id]]
id = "DP-05"
suite = "deployment"
level = "build"
statement = "Deployment orders policy, backup, migration, rollout, readiness, contracts, SLOs, traffic, cleanup, and evidence and stops safely on partial failure."

[[id]]
id = "DP-06"
suite = "deployment"
level = "build"
statement = "Status distinguishes desired, applied, and observed identity; rollback enforces data safety; destroy requires retention policy and authorization."

[[id]]
id = "OP-01"
suite = "operations"
level = "build"
statement = "Modeled OpenTelemetry logs, metrics, traces, resource attributes, and correlation reach an unmodified pinned Collector."

[[id]]
id = "OP-02"
suite = "operations"
level = "build"
statement = "Startup, readiness, liveness, SLIs, SLOs, alerts, and finite evaluation windows are modeled and measured against exact releases."

[[id]]
id = "OP-03"
suite = "operations"
level = "build"
statement = "Backup is accepted only with a successful clean-target restore, integrity proof, application acceptance, and measured RPO and RTO."

[[id]]
id = "OP-04"
suite = "operations"
level = "build"
statement = "Bounded load, dependency, process, network, resource, rollout, credential, telemetry, and registry fault scenarios follow modeled policy."

[[id]]
id = "OP-05"
suite = "operations"
level = "build"
statement = "Observed deployment, health, drift, recovery, and failure evidence remains distinct from proof and binds the deployed release digest."

[[id]]
id = "OP-06"
suite = "operations"
level = "build"
statement = "Sensitive fields, credentials, tokens, operands, history, labels, and secret values are redacted from source-derived and observed evidence."

[[id]]
id = "SC-01"
suite = "supply-chain"
level = "build"
statement = "SPDX 3.0.1 BOMs close over SDK, build, oracle, adapter, application, and runtime dependencies and match OCI closure."

[[id]]
id = "SC-02"
suite = "supply-chain"
level = "build"
statement = "in-toto Statement v1 and SLSA 1.2 provenance bind subjects, builders, sources, parameters, dependencies, and objectively supported build level."

[[id]]
id = "SC-03"
suite = "supply-chain"
level = "build"
statement = "Sigstore verification enforces exact trusted issuer, subject, repository, workflow, ref, environment, certificate, and transparency policy."

[[id]]
id = "SC-04"
suite = "supply-chain"
level = "build"
statement = "Vulnerability and license results identify their immutable inputs and freshness and stale evidence cannot satisfy current policy."

[[id]]
id = "SC-05"
suite = "supply-chain"
level = "build"
statement = "Source, locks, artifacts, plans, OCI content, BOMs, attestations, logs, and evidence are verified free of secret values."

[[id]]
id = "SC-06"
suite = "supply-chain"
level = "build"
statement = "Supply-chain policy rejects wrong subjects or builders, incomplete graphs, tampering, unsigned promotion, and unsupported claims."

[[id]]
id = "TM-01"
suite = "template-ci"
level = "build"
statement = "The versioned UOR template contract preserves repository honesty, anti-vacuity, generated registers, and planted-defect policy."

[[id]]
id = "TM-02"
suite = "template-ci"
level = "build"
statement = "Template-derived devcontainers consume the exact signed SDK manifest digest with no floating feature, installer, action, or host-tool fallback."

[[id]]
id = "TM-03"
suite = "template-ci"
level = "build"
statement = "Full-SHA-pinned actions and reusable workflows invoke the same SDK digest and CLI and return declared release, plan, and evidence digests."

[[id]]
id = "TM-04"
suite = "template-ci"
level = "build"
statement = "Least-privilege jobs build once and pass the exact digest through protected signing, publication, deployment, and verification boundaries."

[[id]]
id = "TM-05"
suite = "template-ci"
level = "build"
statement = "Template checks are read-only and explicit updates produce reviewable downstream patches or pull requests without hidden branch mutation."

[[id]]
id = "TM-06"
suite = "template-ci"
level = "build"
statement = "All in-scope UOR and Prism repositories use the common SDK bootstrap while preserving their repository-specific acceptance gates."
