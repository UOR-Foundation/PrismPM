{
  "name": "PrismPM",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "rust-lang.rust-analyzer",
        "leanprover.lean4"
      ],
      "settings": {
        "editor.formatOnSave": true,
        "[rust]": {
          "editor.defaultFormatter": "rust-lang.rust-analyzer"
        },
        "rust-analyzer.check.command": "clippy",
        "rust-analyzer.procMacro.enable": true,
        "rust-analyzer.cargo.allFeatures": true,
        "search.exclude": {
          "**/target": true
        },
        "files.watcherExclude": {
          "**/target/**": true
        }
      }
    }
  },
  "remoteEnv": {
    "PATH": "/home/codespace/.elan/bin:/home/codespace/.cargo/bin:${containerEnv:PATH}"
  },
  "postCreateCommand": "rustup toolchain install 1.97.1 --profile minimal --component rustfmt --component clippy && cargo install cargo-deny --version 0.20.2 --locked && curl https://elan.lean-lang.org/elan-init.sh -sSf | sh -s -- -y --default-toolchain none && ~/.elan/bin/elan toolchain install leanprover/lean4:v4.32.1"
}

